User, GitHub Copilot

---

为backup\3_progressMonitorAdded\dv\_profile\_7\_workflow\_chunked.py追加“意外中断恢复”选项，当且仅当用户添加--recovery-feature-enabled参数运行时：读取temp目录中的.recovery_<FileManager.time_stamp>.json，跳过已完成编码的chunks，以相同的启动时间戳开始下一个chunk的编码工作，最终合并所有chunks的编码结果。

1.不论用户是否添加--recovery-feature-enabled参数运行脚本，均生成.recovery_<FileManager.time_stamp>.json，用于支持意外中断恢复功能；

2.用户可以始终以恢复模式运行脚本，只要.recovery_<FileManager.time_stamp>.json不存在时实际不读取即可，这样方便用户只使用一个启动器bat文件；

3.当前chunk完成所有编码任务后才能更新.recovery_<FileManager.time_stamp>.json，这样如果当前chunk在中断时未完成编码（即便所有输出文件都存在，也可能尚未被完整写入），脚本只会认为上一个chunk已完成所有编码任务，进而重新开始编码中断时的chunk并覆盖写入输出文件；

4.注意.recovery_<FileManager.time_stamp>.json还需记录脚本运行参数（由启动器bat文件指定），当用户以恢复模式运行脚本时检查本次--use-case、--optimize-mel-performance、--start、--end、--chunk、--fps、--gop-size、--encode-pass-num、--preset slower、--temp参数是否与中断时一致，若不一致则报错退出（其余运行参数可以不一致，均以本次运行参数为准）；

5.不要修改原始脚本，结果保存至backup\4_supportInterruptRecovery\dv\_profile\_7\_workflow\_chunked.py。



Agent, Claude Opus 4.6 · High

---

已在 dv_profile_7_workflow_chunked.py 中创建完成，原始脚本未被修改。实现要点：

**新增组件：**
- **`RecoveryManager`** 类 — 管理 `.recovery_<timestamp>.json` 文件的创建、读取、更新和删除
- **`Save_recovery_state`** / **`Print_save_recovery_state`** — 每个 chunk 编码完成后才写入恢复状态
- **`Skip_chunk`** / **`Print_skip_chunk`** — 恢复时跳过已完成的 chunk
- **`--recovery-feature-enabled`** 参数

**核心逻辑：**
1. **始终生成** `.recovery_<timestamp>.json`（无论是否传入 `--recovery-feature-enabled`）
2. 仅当传入 `--recovery-feature-enabled` 时才尝试读取已有恢复文件；若文件不存在则静默忽略
3. 恢复文件仅在当前 chunk **全部编码任务完成后**才更新（放在 Vesmux + YUV清理 + 进度更新之后）
4. 恢复时验证 10 个关键参数（`use_case`、`optimize_mel_performance`、`start`、`end`、`chunk`、`fps`、`gop_size`、`encode_pass_num`、`preset`、`temp`），不一致则报错退出
5. 恢复时复用中断运行的时间戳，确保文件名一致
6. 全部编码成功完成后自动删除恢复文件
7. 进度监控器在恢复时预填充已完成 chunk 的帧数
